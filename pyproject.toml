[project]
name = "word2vec-numpy"
version = "1.0.0"
description = "Word2Vec Skip-Gram with Negative Sampling in pure NumPy"
requires-python = ">=3.10"
dependencies = ["numpy"]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
